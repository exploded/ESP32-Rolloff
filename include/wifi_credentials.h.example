#pragma once

// Copy this file to wifi_credentials.h and fill in your credentials.
// wifi_credentials.h is excluded from git via .gitignore.
#define WIFI_SSID "YourSSID"
#define WIFI_PASS "YourPassword"
